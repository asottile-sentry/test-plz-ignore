name: debug expr
on:
  issue_comment:
    types: [created]
jobs:
  t:
    runs-on: ubuntu-latest
    name: t
    steps:
    - run: python -c 'import os; print(repr(os.environ["WAT"]))'
      env:
        WAT: ${{format(fromJSON('"\r\n{0}\r\n"'), github.event.comment.body)}}
